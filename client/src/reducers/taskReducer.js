export default function taskReducer(state = [], action) { //TODO sett default state param
    switch(action.type) {
        case 'EDIT' :
            //TODO
        default:
            return state;
    }
}
